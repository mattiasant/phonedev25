<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Phonedev25" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/vintage_brown_light</item>
        <item name="colorPrimaryVariant">@color/vintage_brown</item>
        <item name="colorOnPrimary">@color/text_primary_dark</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/vintage_red_light</item>
        <item name="colorSecondaryVariant">@color/vintage_red</item>
        <item name="colorOnSecondary">@color/text_primary_dark</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnBackground">@color/text_primary_dark</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.FivePapers.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/vintage_brown</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.Phonedev25</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
    </style>

    <!-- Material Button Style -->
    <style name="Widget.FivePapers.Button" parent="Widget.Material3.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- Material Card Style -->
    <style name="Widget.FivePapers.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="contentPadding">16dp</item>
    </style>

</resources>

