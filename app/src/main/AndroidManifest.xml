<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Phonedev25">
        <activity
            android:name=".gameArena"
            android:exported="false" />
        <activity
            android:name=".GameSettingsPage"
            android:exported="false" />
        <activity
            android:name=".StatisticsPage"
            android:exported="false" />
        <activity
            android:name=".StorePage"
            android:exported="false" />
        <activity
            android:name=".Join_Game"
            android:exported="false" />
        <activity
            android:name=".HomePage"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Phonedev25">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PowerUpsActivity"
            android:exported="false" />'
        <activity
            android:name=".AnimationPAge"
            android:exported="false" />
    </application>

</manifest>